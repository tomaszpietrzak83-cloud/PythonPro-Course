answer = """Client is something e. g. application,
browser which sends requests. Server is a program,
which is always ready for requests, than it manage those requests proceeds and sends back."""
client = ("application", "browser")
server = ("computer",)
