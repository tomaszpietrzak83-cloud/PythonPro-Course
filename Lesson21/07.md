# app/models.py
```python
from django.db import models


class Article(models.Model):
    title = models.CharField(max_length=200)
    content = models.TextField()
    pub_date = models.DateTimeField(auto_now_add=True)
    category = models.ForeignKey(Category, on_delete=models.CASCADE)

    def __str__(self):
        return self.title
```

```shell
python manage.py makemigrations
python manage.py migrate
```

```shell
from app.models import Article, Category

sport = Category.objects.get(name="Sport")
technology = Category.objects.get(name="Technology")
culture = Category.objects.get(name="Culture")

articles_data = [
    {
        "title": "How to Start Running",
        "content": "A beginner guide to building a simple running habit without overtraining.",
        "category": sport,
    },
    {
        "title": "Best Home Workout Ideas",
        "content": "A few practical bodyweight exercises you can do at home with no equipment.",
        "category": sport,
    },
    {
        "title": "Healthy Recovery After Exercise",
        "content": "Sleep, hydration, and light stretching can improve recovery after training.",
        "category": sport,
    },
    {
        "title": "Python for Daily Automation",
        "content": "Python can automate repetitive tasks like file renaming and data cleanup.",
        "category": technology,
    },
    {
        "title": "Why Version Control Matters",
        "content": "Git helps teams track changes, collaborate safely, and recover earlier versions.",
        "category": technology,
    },
    {
        "title": "Understanding Web Frameworks",
        "content": "Frameworks like Django provide routing, templates, ORM, and admin tooling.",
        "category": technology,
    },
    {
        "title": "Why Museums Still Matter",
        "content": "Museums preserve memory, context, and access to important cultural works.",
        "category": culture,
    },
    {
        "title": "Books That Shape Imagination",
        "content": "Reading fiction expands vocabulary, empathy, and long-form concentration.",
        "category": culture,
    },
    {
        "title": "The Role of Film in Society",
        "content": "Cinema reflects social values and often influences public conversations.",
        "category": culture,
    },
]

for article_data in articles_data:
    Article.objects.create(**article_data)

```


# app/views.py
```python
from django.shortcuts import render
from app.models import Category


def category_detail_view(request, pk):
    category = Category.objects.get(pk=pk)
    articles = category.article_set.all()

    context = {
        "category": category,
        "articles": articles,
    }

    return render(request, "app/category_detail.html", context)
```

```html
<!-- app/templates/app/category_detail.html -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Category details</title>
</head>
<body>
    <h1>{{ category.name }}</h1>

    <ul>
        {% for article in articles %}
            <li>
                <h2>{{ article.title }}</h2>
                <p>{{ article.content }}</p>
            </li>
        {% empty %}
            <li>No articles in this category yet.</li>
        {% endfor %}
    </ul>
</body>
</html>
```